spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test;MODE=MySQL
    username: sa
    password:
  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: testtest
  access-token:
    expires-in: 86400000
  refresh-token:
    expires-in: 1209600000

oauth:
  apple:
    iss: iss
    client-id: aud