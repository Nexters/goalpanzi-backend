server:
  port: 8080

spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:~/test;MODE=MYSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;
    username: sa
  jpa:
    hibernate:
      ddl-auto: create-drop
  data:
    redis:
      host: localhost
      port: 6379
  servlet:
    multipart:
      max-file-size: 5MB
  quartz:
    job-store-type: jdbc
    jdbc:
      initialize-schema: never
      schema: goalpanzi
    properties:
      org:
        quartz:
          scheduler:
            instanceId: AUTO
            instanceName: quartzScheduler
          jobStore:
            driverDelegateClass: org.quartz.impl.jdbcjobstore.StdJDBCDelegate
            tablePrefix: goalpanzi.QRTZ_
            isClustered: true

jwt:
  secret: ${JWT_SECRET}
  access-token:
    expires-in-days: 1
  refresh-token:
    expires-in-days: 360

springdoc:
  swagger-ui:
    path: /missionmate-docs.html
    query-config-enabled: true
    enabled: true

oauth:
  apple:
    iss: https://appleid.apple.com
    client-id: ${OAUTH_APPLE_CLIENT_ID}

cloud:
  aws:
    credentials:
      access-key: ${STORAGE_ACCESS_KEY_ID}
      secret-key: ${STORAGE_SECRET_KEY}
      bucket: ${STORAGE_BUCKET_NAME}
    region:
      static: ${STORAGE_REGION}
      auto: false
    stack:
      auto: false
    s3:
      endpoint: https://${STORAGE_BUCKET_NAME}.s3.${STORAGE_REGION}.amazonaws.com

firebase:
  enabled: true
  admin-sdk: ${ENCODED_FIREBASE_ADMIN_SDK}